import React from 'react';
import { View, Text } from 'react-native';

interface AppProps {}

const Java: React.FC<AppProps> = () => {
    return (
        <View>
            <Text>Hello, World!</Text>
        </View>
    );
};

export default Java;